apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ml-hhg-demo.fullname" . }}-test-connection"
  labels:
    {{- include "ml-hhg-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ml-hhg-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
